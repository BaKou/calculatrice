import { SoustratDirective } from './soustrat.directive';

describe('SoustratDirective', () => {
  it('should create an instance', () => {
    const directive = new SoustratDirective();
    expect(directive).toBeTruthy();
  });
});
