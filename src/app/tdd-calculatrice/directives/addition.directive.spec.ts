import { AdditionDirective } from './addition.directive';

describe('AdditionDirective', () => {
  it('should create an instance', () => {
    const directive = new AdditionDirective();
    expect(directive).toBeTruthy();
  });
});
