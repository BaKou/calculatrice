import { DiviseDirective } from './divise.directive';

describe('DiviseDirective', () => {
  it('should create an instance', () => {
    const directive = new DiviseDirective();
    expect(directive).toBeTruthy();
  });
});
