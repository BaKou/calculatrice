import { MultiplicateDirective } from './multiplicate.directive';

describe('MultiplicateDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiplicateDirective();
    expect(directive).toBeTruthy();
  });
});
